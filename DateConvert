Function DateConvert(x As Variant) As String
 
    ' Do nothing if the value is not a date.
    If Not IsDate(x) Then Exit Function

    ' Format the date value as a U.S. date formatted string expression.
    DateConvert = Month(x) & "/" & Day(x) & "/" & Year(x)

End Function
